# bin, sysroot, lib, share, and clang-resource-dir are the only copy of
# this custom toolchain. They are the SDK, not a local build cache.
*.wasm
*.tar.gz
*.log
openssl-wasm/build/
wasmtime-46/
# Bootstrap checkout + cmake tree (large; not the installed SDK)
wasi-sdk-src/
wasi-sdk-build/
