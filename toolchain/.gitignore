# The SDK (bin, sysroot, lib, share, clang-resource-dir) is part of this
# repo. Do not ignore it.
*.wasm
*.tar.gz
*.log
openssl-wasm/build/
wasmtime-46/
# Bootstrap checkout + cmake tree (large; not the installed SDK)
wasi-sdk-src/
wasi-sdk-build/
